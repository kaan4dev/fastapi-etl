services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      ETL_DB_HOST: postgres
      ETL_DB_PORT: "5432"
      ETL_DB_NAME: etl
      ETL_DB_USER: etl
      ETL_DB_PASSWORD: etl
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: etl
      POSTGRES_USER: etl
      POSTGRES_PASSWORD: etl
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U etl -d etl"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  pgdata:
